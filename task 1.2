var arr = prompt("Enter your numbers separating by comma").split(",");
arr = arr.map(Number);
var z = prompt("Z");
let total = 0;
var z = parseInt(z);
for (var i = 0; i < arr.length; i++) {
  if (arr[i] >z) { arr[i]=z;
    total += arr[i];
  }
}
console.log(total);
