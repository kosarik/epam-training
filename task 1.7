var arr = prompt("Enter your numbers separating by comma").split(",");
arr = arr.map(Number);
for (let i = 0, j = arr.length - 1; i < j; i++, j--) {
        var max = arr[i] + arr[j];
    } console.log(max);
